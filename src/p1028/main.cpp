#include <iostream>

using namespace std;


int main() {
	int n, count;
	string maxName, minName, name, maxDate, minDate, date;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> name >> date;
		
	}
	return 0;
}
